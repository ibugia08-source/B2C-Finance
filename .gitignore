node_modules
.next
.env
.env.*
!.env.example
*.db
*.db-journal
.DS_Store
.vscode/
*.code-workspace
prisma/migrations/dev.db*

# backups locais do banco
dev.db.bak-*
*.bak

# cache incremental do TypeScript
tsconfig.tsbuildinfo
.vercel

# backups dos scripts de correção de dados
backups/
.uploads
.env*
# ...exceto os modelos sem segredo (o .env* acima os pegaria de volta)
!.env.example
!.env.development.example
!.env.staging.example

# Documentação consolidada gerada (derivada de docs/*.md — não versionar)
docs/B2C-Finance-Documentacao-Consolidada.md
docs/B2C-Finance-Documentacao-Consolidada.html

# ===== Duplicatas do iCloud Drive =====
# O sincronismo cria cópias "arquivo 2.ext" ao resolver conflitos. Elas nunca
# devem entrar no repositório: são idênticas ao original e poluem todo diff.
*\ 2.ts
*\ 2.tsx
*\ 2.js
*\ 2.mjs
*\ 2.md
*\ 2.sql
*\ 2.json
*\ 2.css
.backups/
# Relatórios gerados com dados de clientes — ficam fora do repositório.
relatorios-julho/

# PDFs gerados pela verificação de relatórios: são para COMPARAR localmente
# com docs/pdfs-originais da auditoria, e carregam nome, valor e situação de
# clientes reais do banco de trabalho. Ficam no disco, nunca no repositório —
# mesma regra de .backups/ e relatorios-julho/.
docs/pdfs-v2/

# Documentação gerada (refaça com: node scripts/docs/gerar.mjs)
docs/gerado/
